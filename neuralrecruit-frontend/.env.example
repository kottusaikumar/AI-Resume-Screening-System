# URL of the RecruitIQ FastAPI backend.
# Default (if unset): http://localhost:8000
VITE_API_URL=http://localhost:8000

# Must match the backend's API_KEY (see recruitiq-backend/.env.example).
# Leave blank if the backend has no API_KEY set (local-only dev).
VITE_API_KEY=
